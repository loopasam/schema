# Application dependencies

require:
       - play
       - provided -> schema 1.0

repositories: 
       - provided: 
           type:       local 
           artifact:   "${application.path}/jar/[module]-[revision].zip"
           contains: 
               - provided -> * 